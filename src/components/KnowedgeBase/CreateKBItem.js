import React, { Component } from 'react'
import axios from 'axios' //Biblioteca que me permite realizar peticiones HTTP
export default class CreateKBItem extends Component {
    
    //llama esta funcion cuando el componente ha sido montado
   async componentDidMount(){

        await axios.get
    }
    
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
